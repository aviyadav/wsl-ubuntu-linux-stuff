# Ubuntu on WSL with Desktop env

#### [Ubuntu - WSL2]

```
sudo apt update
sudo apt dist-upgrade
```

```
sudo apt install tasksel
sudo tasksel
```

#### [Choose the desktop]

[Linux - UI]

Choose KDE Plasma

<ok>

```
sudo apt install xrdp

--- get ip ubuntu --
ip -br a
```

choose IP (eth0) - 172.27.133.166

[WINDOWS]

start remote desktop connection
	enter the ip
	connect
